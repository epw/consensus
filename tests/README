These tests confirm some basic things about the PDFs generated by the
Makefile.

They require pdfminer installed for Python 3, which you can likely do
with:
  $ sudo pip3 install pdfminer

Run the tests with:
  $ python3 playbooks.py
